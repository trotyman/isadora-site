# Copie este arquivo para .env e preencha os valores

# Web3Forms
WEB3FORMS_ACCESS_KEY=COLOQUE_SUA_CHAVE_AQUI

# JWT Secret (gere um novo com: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))")
JWT_SECRET=sua_chave_secreta_aqui

# Vercel KV (adicionado automaticamente ao conectar o KV no dashboard da Vercel)
# KV_URL=
# KV_REST_API_URL=
# KV_REST_API_TOKEN=
# KV_REST_API_READ_ONLY_TOKEN=

# ==================================
# Cloudflare R2 Storage
# ==================================
# ID da conta Cloudflare (encontre em: Dashboard → R2 → sidebar direita, ou na URL)
R2_ACCOUNT_ID=seu_account_id_aqui

# Credenciais de API (crie em: R2 → Manage R2 API Tokens → Create API Token)
# Use permissão "Object Read & Write" para maior segurança
R2_ACCESS_KEY_ID=seu_access_key_id_aqui
R2_SECRET_ACCESS_KEY=sua_secret_access_key_aqui

# Nome do bucket criado no R2
R2_BUCKET_NAME=isadora-uploads

# URL pública do bucket (já configurada)
R2_PUBLIC_URL=https://pub-c223da626df34d90984474e07d938a70.r2.dev
